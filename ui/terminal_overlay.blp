using Gtk 4.0;

template $TerminalOverlay: $BaseOverlay {
  layer-namespace: "LayerShell/Terminal";
  keyboard-exclusive: true;
  visible: bind template.model as <$IOModel>.overlays as <$OverlaysModel>.terminal;
  width-request: 1000;
  height-request: 700;
}
